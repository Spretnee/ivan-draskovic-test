import {StyleSheet} from 'react-native';
import {FONT_DARK2} from '../../../constants/colors';

export const styles = StyleSheet.create({
  container: {alignItems: 'flex-start'},
  text: {textTransform: 'capitalize', color: FONT_DARK2},
});
