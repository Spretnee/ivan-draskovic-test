import React from 'react';
import {SvgXml} from 'react-native-svg';
import {BACK_BUTTON} from '../../../assets/images/svg';

export const BackButton = () => {
  return <SvgXml onPress={() => {}} xml={BACK_BUTTON} />;
};
